#include <bits/stdc++.h>

using namespace std;

int n, q;

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    cin >> n >> q;

    cout << n << ',' << q << '.';

    while (q--)
    {
        int a, b, c;

        cin >> a >> b >> c;

        cout << a << ',' << b << ',' << c << '.';

    }
}