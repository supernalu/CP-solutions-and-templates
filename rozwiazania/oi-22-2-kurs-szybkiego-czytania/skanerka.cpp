#include <bits/stdc++.h>

using namespace std;

int n, a, b, p, m;

string pattern;

int main()
{
    cin >> n >> a >> b >> p >> m;

    cin >> pattern;

    cout << n << ',' << a << ',' << b << ',' << ',' << p << ',' << m << ',' << pattern;
}