#include<bits/stdc++.h>

using namespace std;

int t;

int main()
{
    cin >> t;

    while (t--)
    {
        string a, b;

        cin >> a >> b;

        int res = a.size() + b.size();

        for (int i = 0; i < a.size(); i++)
        {
            string sub = "";

            for (int j = i; j < a.size(); j++)
            {
                sub += a[j];

                for (int k = 0 ; )
            }
        }
    }
}