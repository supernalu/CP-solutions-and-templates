#include <bits/stdc++.h>

using namespace std;
constexpr int MAXN = 2e5 + 10;
int n;
char s[MAXN], t[MAXN];

int main()
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    cin >> n >> s+1 >> t+1;

    for (int i = n-1; i > 0 )
}